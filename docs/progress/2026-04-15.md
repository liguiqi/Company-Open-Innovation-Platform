# 2026-04-15 部署记录

## 完成事项

- 基于 `c573b20` 重新收敛品牌实现，不再沿用之前过宽的头部 Logo 方案
- 新增 `public/branding/het-full.png` 作为统一品牌 PNG 资源
- 首页头部改为小尺寸 PNG Logo + 文本副标题的组合布局
- 首页页脚改为白色反相品牌 PNG，贴近视觉参考稿
- 登录页左侧品牌位同步切换为 PNG
- 工作台侧边栏左上角品牌位同步切换为 PNG，并与官网头部保持同源视觉
- 工作台左侧导航激活态与底部说明卡从纯黑色块调整为浅蓝品牌层次
- 全站卡片、按钮、输入框的超大圆角统一收紧，改为更硬朗的分级圆角体系
- 全站阴影、悬浮位移与玻璃模糊感同步下调，减少模板化和 AI 感
- 修复 standalone 运行目录静态资源不同步导致的首页 CSS 500 问题，并补充 `media/` 同步
- 修复评审保存后表单状态流转未同步的问题，并补强提案提交接口的附件格式校验与前端错误兜底
- 追加修复评审详情页客户端状态残留问题：在详情页按 `proposal.id + status + reviewNotes` 重新挂载评审表单，避免 `router.refresh()` 后下拉框保留旧值
- 调整 `tech-needs.needId` 生成规则：后台新建需求时改为系统自动生成 `RD-YYYY-流水号`，按当年已有最大编号递增，字段改为只读展示
- 仓库内补充 `systemd` 样例，并将 `innovation` 的 nginx 代理端口修正为 `3005`

## 当前状态

- `innovation-platform.service`：运行中
- `nginx.service`：运行中
- `innovation.example.com`：可通过 `443` 访问当前项目
- `bothub.example.com`：代理已命中上游，但上游应用当前拒绝该 Host，返回 `403`

## 补充说明

- `bothub` 的 `403` 不是 Nginx 问题，而是其上游 Vite 应用未放行 `bothub.example.com`
- 当前项目若更新代码后重新构建，可通过 `sudo systemctl restart innovation-platform.service` 完成服务重启
