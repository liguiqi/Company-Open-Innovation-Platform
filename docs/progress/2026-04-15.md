# 2026-04-15 部署记录

## 完成事项

- 为 `innovation-platform` 增加 `systemd` 服务配置，固定监听 `3005`
- 安装并启用 `nginx`
- 将 `innovation.example.com` 反向代理到 `127.0.0.1:3005`
- 将 `bothub.example.com` 反向代理到 `127.0.0.1:3000`
- 验证 `innovation` 域名 HTTPS 返回 `200`
- 将前台、登录页、工作台左上角品牌位改为直接加载Open Innovation PNG Logo，非核心品牌位恢复为文字
- 将 `docs/testing.md` 扩展为可执行的完整验收文档
- 修复 `standalone` 启动时未同步 `media/` 目录的问题，避免案例封面和附件文件缺失

## 当前状态

- `innovation-platform.service`：运行中
- `nginx.service`：运行中
- `innovation.example.com`：可通过 `443` 访问当前项目
- `bothub.example.com`：代理已命中上游，但上游应用当前拒绝该 Host，返回 `403`

## 补充说明

- `bothub` 的 `403` 不是 Nginx 问题，而是其上游 Vite 应用未放行 `bothub.example.com`
- 当前项目若更新代码后重新构建，可通过 `sudo systemctl restart innovation-platform.service` 完成服务重启
