# 2026-04-15 部署记录

## 完成事项

- 基于 `c573b20` 重新收敛品牌实现，不再沿用之前过宽的头部 Logo 方案
- 新增 `public/branding/het-full.png` 作为统一品牌 PNG 资源
- 首页头部改为小尺寸 PNG Logo + 文本副标题的组合布局
- 首页页脚改为白色反相品牌 PNG，贴近视觉参考稿
- 登录页左侧品牌位同步切换为 PNG
- 工作台侧边栏左上角品牌位同步切换为 PNG，并与官网头部保持同源视觉
- 工作台左侧导航激活态与底部说明卡从纯黑色块调整为浅蓝品牌层次
- 全站卡片、按钮、输入框的超大圆角统一收紧，改为更硬朗的分级圆角体系
- 全站阴影、悬浮位移与玻璃模糊感同步下调，减少模板化和 AI 感
- 修复 standalone 运行目录静态资源不同步导致的首页 CSS 500 问题，并补充 `media/` 同步
- 修复评审保存后表单状态流转未同步的问题，并补强提案提交接口的附件格式校验与前端错误兜底
- 追加修复评审详情页客户端状态残留问题：在详情页按 `proposal.id + status + reviewNotes` 重新挂载评审表单，避免 `router.refresh()` 后下拉框保留旧值
- 调整 `tech-needs.needId` 生成规则：后台新建需求时改为系统自动生成 `RD-YYYY-流水号`，按当年已有最大编号递增，字段改为只读展示
- 修复 Payload Admin 登录页在 `admin/login` 的泛白问题，并为公开站补充基于 `prefers-color-scheme` 的深浅主题变量与组件适配
- 仓库内补充 `systemd` 样例，并将 `innovation` 的 nginx 代理端口修正为 `3005`
- 工作台首页 `/dashboard` 补齐暗色主题适配：侧边栏、顶部操作区、统计卡片与最近方案列表全部切到统一主题变量，避免黑底下出现突兀的纯黑卡片和深色文字
- 官网页脚版权信息后统一为 `Powered by LGQ | Ver 2026.04`
- 登录页 `/login` 背景图层增强，提高底图可见度并削弱过厚遮罩，减少“发白”感
- 登录页密码模式收敛为“邮箱 / 手机”登录，前端文案、参数校验与后端账号解析同步移除用户名登录入口
- Payload `admin/unauthorized` 页面补充暗色按钮与说明文字适配，修正 `Log out` 按钮在深色环境下过亮、过突兀的问题
- 验收联调账号 `lgq / admin@example.com` 的手机号数据校正为 `13800000000`，以匹配当前验收约定并完成手机号密码登录验证
- 公开页品牌文案中的 `H&T` 统一替换为 `HeT`
- 登录页底部追加居中的版权与开发者声明，适配 `innovation.example.com` 与内网 `10.0.0.2:3005`
- 新增全局明暗主题切换能力：通过 `data-theme + localStorage` 持久化控制，公开站标题栏和工作台品牌区均增加图片式 `toggle`
- 将 `dashboard/proposals`、`dashboard/proposals/[id]`、`dashboard/proposals/new`、`dashboard/settings`、`dashboard/partners`、`dashboard/users` 及其表单组件统一改为主题变量驱动，消除深色模式下残留的白底黑字冲突
- 本地引入 `public/theme/toggle-light.png` 与 `public/theme/toggle-dark.png`，避免主题切换依赖外站图片加载
- 补充 `src/scripts/refresh-branding.ts`，用于把库内已发布案例摘要、封面 SVG 与种子品牌文案中的残留 `H&T` 收敛为 `HeT`
- 收敛首页头部交互：删除品牌区左侧重复的主题切换按钮，仅保留操作区右侧 toggle，并将头部主 CTA 从“提交创新方案”缩短为“提交方案”
- 提案上传链路追加 `txt` 文本附件支持，并在前端明确“可一次选择多个文件”的上传提示
- Payload 后台补强附件可见性：`proposals` 默认列表列加入 `attachments`，`media` 默认列表列加入 `proposal / uploadedBy`，便于管理员追踪来源与复用文档媒体
- 工作台桌面端布局改为“左侧栏固定、右侧内容独立滚动”，同时将左上品牌卡从侧栏滚动区拆出，确保品牌位与主题切换不随方案内容滚动
- 提案表单附件区改为显式状态管理：提交时手工拼装 `FormData`，并在前端以紧凑表格展示已选文件名、大小和逐项取消按钮，同时对同名同尺寸文件做去重
- 文档附件下载链路补强：`media` 对 `purpose=document` 的文件统一返回附件下载响应头，提案详情页链接同步增加 `download` 属性，避免 `.txt` 在浏览器中直接预览并出现中文乱码
- 进一步补充业务侧附件下载 API：工作台提案详情页改为走 `/api/attachments/[id]`，复用 `innovation-session` 会话鉴权并强制返回下载响应，绕开 Payload 原生文件路由与业务登录态不互通的问题
- 公开页标题说明排版收口：居中页的说明文案自动 `mx-auto`，`/cases` 页面说明在桌面端强制单行展示
- 官网页脚与登录页底部版权声明统一更新为 `Powered by LGQ | Ver 2026.04`
- 全站增加统一页面转场：内部链接点击叠加顶部进度线与轻量遮罩，公开页、登录页、工作台以及程序化跳转的登录/退出/提交动作都补充入场动画
- Payload Admin 登录页通过 `admin.components.graphics.Logo` 切换为 HeT 全尺寸 PNG，并针对 light / dark 主题分别展示蓝色 / 反白版本
- 转场范围进一步收窄到内容区：公开站保留头尾静态、登录页保留左侧品牌信息静态、工作台保留左侧侧栏与顶部栏静态，只对右侧内容区域做切页动画
- 修复附件持久化目录与 standalone 运行目录不一致的问题：上传统一落到仓库根目录 `media/`，启动同步时先把旧 `.next/standalone/media/` 回灌到持久目录，再同步到 standalone，避免服务重启后数据库仍有记录但磁盘文件丢失
- 修复中文附件文件名的下载响应头编码问题：统一改为 ASCII 回退名 + `filename*` UTF-8 编码，避免 `开放创新平台.txt`、`《选题说明（模板与示例）》.docx` 这类文件在下载时触发 500
- 对历史已丢失的附件记录做了现场回填：已补回 `OI-test.txt`、`开放创新平台.txt`、`《选题说明（模板与示例）》.docx` 到根目录 `media/`，`OI-test-1.txt` 仍未在机器上找到原始文件，需要业务侧重新上传

## 当前状态

- `innovation-platform.service`：运行中
- `nginx.service`：运行中
- `innovation.example.com`：可通过 `443` 访问当前项目
- `bothub.example.com`：代理已命中上游，但上游应用当前拒绝该 Host，返回 `403`

## 补充说明

- `bothub` 的 `403` 不是 Nginx 问题，而是其上游 Vite 应用未放行 `bothub.example.com`
- 当前项目若更新代码后重新构建，可通过 `sudo systemctl restart innovation-platform.service` 完成服务重启
